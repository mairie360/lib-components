name: Lib Components CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  pages: write
  id-token: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/front-libs-cicd.yml@v2.3.0
    with:
      cicd_version: v2.2.0
    secrets: inherit
