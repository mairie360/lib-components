name: Orchestrator Pipeline

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-pipeline:
    uses: mairie360/CICD/.github/workflows/front-libs-cicd.yml@feat/MAIR-3-add-lib-component-cicd
    secrets: inherit
